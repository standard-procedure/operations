module Operations
  # Version is shared across all gems in the mono-repo
  # Major.Minor versions are synchronized across core + adapters
  # Patch versions can diverge for adapter-specific fixes
  VERSION = "2.0.0.alpha1"
end
